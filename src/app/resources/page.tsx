import { ResourcesIndex } from "./_components/ResourcesIndex";

export const metadata = {
  title: "Resources — Scalixity",
  description:
    "Expand your AI knowledge with our curated guides, whitepapers, webinars, and workshops.",
};

export default function ResourcesPage() {
  return <ResourcesIndex />;
}
