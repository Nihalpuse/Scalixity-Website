import { AboutView } from "./_components/AboutView";

export const metadata = {
  title: "About us — Scalixity",
  description:
    "Scalixity started with late nights, first clients, and high standards. Meet the team designing and engineering AI products across industries — partners, not vendors, who care about doing things right.",
};

export default function AboutPage() {
  return <AboutView />;
}
